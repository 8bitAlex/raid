---
sidebar_position: 1
slug: /overview
---

# Overview

Raid is a CLI tool that lets you define your entire development environment — repositories, install steps, environment configs, and team commands — in a single YAML profile. Check it in, share it with the team, and anyone can go from a blank machine to a fully running environment with one command.

## Key Concepts

### Profiles

A **profile** is a YAML file that describes everything about your development environment: which repositories to clone, what to run during setup, how to handle environments, and what commands are available to the team.

Profiles are shared — check one into a repo, host it at a URL, or distribute it however your team prefers. When a teammate runs `raid profile add`, they get the same setup as everyone else.

```yaml
name: "platform"

repositories:
  - name: "api"
    url: "git@github.com:my-org/api.git"
    path: "~/dev/api"
  - name: "frontend"
    url: "git@github.com:my-org/frontend.git"
    path: "~/dev/frontend"
```

→ See [Profile Configuration](/docs/features/profiles)

---

### Environments

**Environments** are named configurations — sets of variables and tasks — that can be applied across all repositories at once. Switching from local to staging is a single command: `raid env staging`.

Each environment can write `.env` files, run setup scripts, or prompt for confirmation before applying. Repositories define their own environment variables in their `raid.yaml`, which are merged with the profile-level environment when applied.

```yaml
environments:
  - name: "local"
    variables:
      - name: "LOG_LEVEL"
        value: "debug"
    tasks:
      - type: Shell
        cmd: "docker-compose up -d db"
  - name: "production"
    tasks:
      - type: Confirm
        message: "Switch to production?"
```

→ See [Environments](/docs/features/environments)

---

### Tasks

**Tasks** are the unit of work in raid. They appear in install steps, commands, and environment definitions. Every task has a `type` and type-specific fields.

Raid ships with task types for running shell commands, rendering templates, prompting for input, waiting for services, performing git operations, and more.

```yaml
tasks:
  - type: Shell
    cmd: "npm install"
  - type: Wait
    url: "http://localhost:3000/health"
    timeout: "30s"
  - type: Print
    message: "Ready."
    color: "green"
```

→ See [Task Types](/docs/features/tasks)

---

### Commands

**Commands** are custom CLI commands defined in your profile or in a repository's `raid.yaml`. Once defined, they're available to everyone on the team as `raid <name>`.

Use commands to encode team workflows — deployments, migrations, test runs — so no one has to remember the exact steps or ask a colleague. Commands accept arguments, support output redirection, and can run tasks in parallel.

```yaml
commands:
  - name: "deploy"
    usage: "Deploy all services to staging"
    tasks:
      - type: Shell
        cmd: "./scripts/deploy.sh $RAID_ARG_1"
        path: "~/dev/api"
```

→ See [Custom Commands](/docs/usage/custom)

---

### Task groups

**Task groups** are reusable sequences of tasks defined once at the profile level and referenced by name from commands, install steps, or environments. They keep configuration DRY and support parallel execution and automatic retries.

```yaml
task_groups:
  install-deps:
    - type: Shell
      cmd: "npm install"
      path: "~/dev/api"
    - type: Shell
      cmd: "npm install"
      path: "~/dev/frontend"

commands:
  - name: "setup"
    tasks:
      - type: Group
        ref: "install-deps"
```

→ See [Task Groups](/docs/features/task-groups)

---

### Variables

**Variables** let you pass dynamic values between tasks. Raid resolves `$VAR` and `${VAR}` references using a priority chain: `Set` tasks override shell exports, which override OS environment variables.

```yaml
tasks:
  - type: Set
    var: "ENVIRONMENT"
    value: "staging"
  - type: Shell
    cmd: "deploy --env=$ENVIRONMENT"
```

→ See [Variables](/docs/features/variables)

---

### Per-repo configuration

Repositories can define their own commands, environments, and install steps by committing a `raid.yaml` at their root. These are automatically merged into the active profile.

```yaml title="~/dev/api/raid.yaml"
name: "api"
branch: "main"

commands:
  - name: "migrate"
    tasks:
      - type: Shell
        cmd: "go run ./cmd/migrate"
```

→ See [Repository Configuration](/docs/features/repo-config)

---

## Next steps

- [Getting Started](/docs/start) — install raid and set up your first profile
- [Profile Configuration](/docs/features/profiles) — full profile file format
- [Task Types](/docs/features/tasks) — everything a task can do
- [Variables](/docs/features/variables) — variable resolution and expansion
- [Command Reference](/docs/references/commands) — built-in commands
