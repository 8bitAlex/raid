# yaml-language-server: $schema=./schemas/raid-repo.schema.json

name: raid
branch: main

environments:
  - name: dev
    tasks:
      - type: Shell
        cmd: echo "Hello, world! - From raid"
      - type: Script
        path: ./docs/examples/hello.sh