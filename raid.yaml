# Uses local schema for development
# yaml-language-server: $schema=./schemas/raid-repo.schema.json

# Required for YAML support in VSCode
# https://marketplace.visualstudio.com/items?itemName=redhat.vscode-yaml

name: raid
branch: main
